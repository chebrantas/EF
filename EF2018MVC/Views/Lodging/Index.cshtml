@model IEnumerable<EF2018MVC.Models.Lodging>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Owner)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsResort)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MilesFromNearestAirport)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LocationId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Owner)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsResort)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MilesFromNearestAirport)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LocationId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LodgingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LodgingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LodgingId })
        </td>
    </tr>
}

</table>
